1. MVP:   
* Inputs: A single number input for password length (min 6, max 20).

* Outputs: Generate two unique passwords displayed in separate, read only fields.

* Implement toggles for symbols, numbers, and uppercase letters. 

* User Interaction:
    * Generate: Button click triggers the password generation.

    * Copy: Clicking a generated password field instantly copies it to the clipboard.

* TODO: A dark/light mode toggle that changes the app's theme. A better copy-to-clipboard button.  
Better layout for slider and characters toggle.